[package]
name = "paxlovid-standalone"
version = "1.0.0"
edition = "2021"
authors = ["TMU ER"]
description = "Paxlovid Native Standalone Checker without WebView2"

[dependencies]
eframe = "0.29"
arboard = "3.4"
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
regex = "1.10"

[build-dependencies]
